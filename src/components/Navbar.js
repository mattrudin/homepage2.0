import React from 'react'

const Navbar = props => (
    <nav className="NavBar" >
        <span>Home</span>
        <span>Portfolio</span>
        <span>About</span>
    </nav>
)

export default Navbar